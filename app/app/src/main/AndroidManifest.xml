<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <uses-sdk tools:overrideLibrary="org.tensorflow.lite.support" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:theme="@style/Theme.ForMyKids">

        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".auth.WelcomeActivity" android:exported="false" />
        <activity android:name=".auth.OnboardingActivity" android:exported="false" />
        <activity android:name=".auth.RoleSelectActivity" android:exported="false" />
        <activity android:name=".auth.PairingActivity" android:exported="false" />

        <activity android:name=".ModeSelectActivity" android:exported="false" />
        <activity android:name=".parent.ParentActivity" android:exported="false" />
        <activity android:name=".child.ChildActivity" android:exported="false" />

        <service
            android:name=".child.AudioStreamService"
            android:foregroundServiceType="microphone"
            android:exported="false" />

        <service
            android:name=".child.DangerDetectionService"
            android:foregroundServiceType="microphone"
            android:exported="false" />

        <service
            android:name=".parent.AudioListenService"
            android:foregroundServiceType="mediaPlayback"
            android:exported="false" />

        <activity android:name=".billing.PaywallActivity" android:exported="false" />
        <activity android:name=".settings.SettingsActivity" android:exported="false" />

        <service
            android:name=".FcmService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <receiver
            android:name=".child.DetectionScheduleReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="com.formykids.SCHEDULE_START_DETECTION" />
                <action android:name="com.formykids.SCHEDULE_STOP_DETECTION" />
            </intent-filter>
        </receiver>

    </application>
</manifest>
